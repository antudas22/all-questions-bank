export const metadata = {
    title: "Board Questions - AQB"
}

const BoardQuestions = () => {
    return (
        <div>
            Board Questions
        </div>
    );
};

export default BoardQuestions;